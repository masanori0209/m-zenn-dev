#!/bin/sh
set -eu

if [ -n "${ARTICLE_CHECK_BASE:-}" ]; then
  BASE="$ARTICLE_CHECK_BASE"
else
  BASE="@{u}"
fi

if ! git rev-parse --verify "$BASE" >/dev/null 2>&1; then
  BASE="origin/main"
fi

npm run check:published-strict -- --published-diff-base="$BASE"
